## funcion min(x, y)
function @min:num (x:num, y:num)
    var minn:bool;
  {
  when ((x < y) == true) do return x++;
  return y;
  }

## funcion max(x, y)
function @max:num (x:num, y:num)
  {
  if ((x < y) == false) do
    {
    return x;
    }
  else
    {
    return y;
    }
  }

print @min(x++,2);
x++;
x:=y+3;
print @max(++x,false);
end